# CLAUDE.md — coven-reach

**Read [`AGENTS.md`](AGENTS.md).** It is the canonical guide for AI agents
(including Claude Code) contributing to this repo — the crate structure, the
security/secret invariants, the branch/PR workflow, local CI gates
(`cargo fmt/clippy/test`), and contributor attribution.

Claude Code auto-loads this file; everything you need lives in `AGENTS.md`.
There is no separate Claude-only workflow — follow `AGENTS.md`.
